export const metadata = { title: 'DOD SBIR/STTR Scanner — ShareOS' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>;
}
